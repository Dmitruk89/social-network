import styles from './Dialogs.module.scss';

const Dialogs = (props) => {
    return (
        <div className={styles.dialogs}>
            <div>dialogs</div>
            <div>chat area</div>
        </div>
    )
}

export default Dialogs;